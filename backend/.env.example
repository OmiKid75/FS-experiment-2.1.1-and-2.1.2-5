# Copy this file to .env (or export these as real environment variables)
# and never commit the real .env file.

MONGODB_URI=mongodb://localhost:27017/helpdesk
SERVER_PORT=8080

# If using MongoDB Atlas instead of a local instance:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/helpdesk
